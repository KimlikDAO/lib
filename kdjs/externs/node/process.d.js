/** @externs */

/** @const */
const process = {};

/**
 * @param {number} exitCode
 */
process.exit = (exitCode) => { }

/**
 * @param {string} event
 * @param {function(*)} handler
 */
process.on = (event, handler) => { }

/** @const {!Array<string>} */
process.argv;
